package com.crossfit.timer.util

object Constants {
    // Timer defaults
    const val DEFAULT_COUNTDOWN_SECONDS = 3
    const val DEFAULT_BEEP_LAST_SECONDS = 10

    // AMRAP defaults
    const val DEFAULT_AMRAP_MINUTES = 20

    // EMOM defaults
    const val DEFAULT_EMOM_ROUNDS = 10
    const val EMOM_INTERVAL_SECONDS = 60

    // For Time defaults
    const val DEFAULT_TIME_CAP_MINUTES = 30

    // Custom defaults
    const val DEFAULT_WORK_SECONDS = 40
    const val DEFAULT_REST_SECONDS = 20
    const val DEFAULT_CUSTOM_ROUNDS = 8

    // Sounds
    const val BEEP_SHORT = "beep_short"
    const val BEEP_LONG = "beep_long"
    const val BEEP_ROUND = "beep_round"
    const val COUNTDOWN_BEEP = "countdown_beep"
    const val COUNTDOWN_GO = "countdown_go"
    const val FINISH_SOUND = "finish_sound"
}
